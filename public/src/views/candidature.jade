div(
  layout='row'
  layout-align='center center'
  layout-xs="column"
  style='min-height: 300px;'
  ng-cloak='')
    md-content( flex="50" flex-xs="100" layout-padding )
        form( name='candidature' ng-submit="save()")

            h3 Faites vos choix de club pour l'année prochaine

            md-input-container.md-block
                label Premier choix
                md-select(
                    name="choices[0]"
                    ng-model="choices[0]"
                    ng-disabled="!clubs"
                    required )
                    md-option(
                        ng-repeat="club in clubs | orderBy: 'club_name' "
                        ng-disabled="isDisabled(club)"
                        ng-value="club" ) {{club.club_name}}
                //div( ng-messages="candidature.choice[0].$error" )
                    div( ng-message="required" )
                        | Requis

            md-input-container.md-block
                label Deuxième choix
                md-select(
                    name='choices[1]'
                    ng-model='choices[1]'
                    ng-disabled='!clubs'
                    required)
                    md-option(
                        ng-repeat="club in clubs | orderBy: 'club_name'"
                        ng-disabled='isDisabled(club)'
                        ng-value='club') {{club.club_name}}
                //div( ng-messages="candidature.choices[1].$error" )
                    div( ng-message="required" )
                        | Requis

            md-input-container.md-block
                label Troisième choix
                md-select(
                    name='choices[2]'
                    ng-model='choices[2]'
                    ng-disabled='!clubs'
                    required)
                    md-option(
                        ng-repeat="club in clubs | orderBy: 'club_name'"
                        ng-disabled='isDisabled(club)'
                        ng-value='club' ) {{club.club_name}}
                //div( ng-messages="candidature.choices[2].$error" )
                    div( ng-message="required" )
                        | Requis


        div(layout='row')
            md-button( type='submit' ng-click='save()' ) Sauvegarder
            md-button.md-primary(
                ng-click='clearValue()'
                layout=''
                layout-align='center end') Annuler

    md-content( flex="50" flex-xs="100" layout-padding )
        md-card
            md-card-title
                md-card-title-text
                    span( class="md-headline" ) Capisen
            md-card-content( layout="row" layout-align="space-between" )
                | Vous souhaitez postuler pour Capisen ?
            md-card-actions( layout="row" layout-align="start center" )
                md-button.md-raised.md-primary( ng-click="goCapisen()" ) Signaler ma demande à Capisen

